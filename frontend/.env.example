# Local/non-Vercel server-side proxy only. Browser calls always use same-origin /api.
# Vercel Services routes /api directly to backend via root vercel.json; no BACKEND_URL
# or NEXT_PUBLIC API URL is required there. Never set BACKEND_URL to the public app URL.
BACKEND_URL=http://127.0.0.1:8000
